MODULE syun1(SYSMODULE)
    ! comment
    VAR string test := "pippo !pluto";
    VAR num n1 := 0;
    PERS pone pose1;
    VAR orient o1 := [1, 0, 0, 0];
    LOCAL PROC init()
    o1.q1 := -1;
    ENDPROC
ENDMODULE
! end comment